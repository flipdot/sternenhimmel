[project]
name = "sternenhimmel"
version = "0.0.0"
description = " Connects switches with lights via zigbee2mqtt."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "paho-mqtt==1.6.1",
    "sentry-sdk==2.8.0",
]
